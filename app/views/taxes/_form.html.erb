<%= simple_form_for(@tax) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :value %>%
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
